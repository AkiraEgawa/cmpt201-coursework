#define _POSIX_C_SOURCE 200112L
#define _GNU_SOURCE
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <unistd.h>

struct header {
  uint64_t size;
  struct header* next;
};

int main() {
  intptr_t increment = 256;
  void* start = sbrk(0);
  printf("start: %p\n",start);
  void* end = sbrk(increment);
  printf("end: %p\n",end);
  return 0;
}
